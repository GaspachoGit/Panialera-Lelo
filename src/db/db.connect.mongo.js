/* const mongoose = require('mongoose')


const dbConnect = (user, pass) =>{
	mongoose.set('strictQuery', false);
	mongoose.connect(`mongodb+srv://${user}:${pass}@cluster0.yehvjpt.mongodb.net/?retryWrites=true&w=majority`, (err)=>{
		if(err) console.log(err)
	})
}

module.exports = dbConnect */